# _stack Playbook Adoption

`_stack` is a remote workflow/operator consumer of the Playbook convergence contract. It consumes the owner export at `repos/playbook/exports/playbook.contract.example.v1.json` (owner ref `main`) without copying, certifying, or owning Playbook doctrine.

The declared profile is `playbook_convergence_contract` version `1.0.0`. This repo-owned evidence covers only `_stack` workflow/operator surfaces: its adoption export, targeted Node test, and targeted verification report.

Existing `docs/STACK-ORCHESTRATION-ADOPTION.md` and `ops/stack/Test-StackAdoptionContracts.ps1` remain the authoritative `_stack` consumer and conformance evidence. This note does not claim repo-wide Playbook certification and does not change ATLAS-root adoption classification.
